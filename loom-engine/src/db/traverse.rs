// loom-engine/src/db/traverse.rs
// Graph traversal — calls loom_traverse SQL function (1-2 hop, cycle-safe).

// TODO: Implement
// - traverse(pool, entity_id, max_hops, namespace) -> Vec<TraversalResult>
// - Wraps the loom_traverse PostgreSQL function
