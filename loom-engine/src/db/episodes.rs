// loom-engine/src/db/episodes.rs
// Episode CRUD queries — compile-time checked via sqlx.

// TODO: Implement
// - insert_episode(pool, episode) -> Episode
// - get_episode(pool, id) -> Option<Episode>
// - find_by_content_hash(pool, hash) -> Option<Episode>
// - list_unprocessed(pool, limit) -> Vec<Episode>
// - mark_processed(pool, id, extraction_metrics)
// - search_by_embedding(pool, embedding, namespace, limit) -> Vec<Episode>
