// loom-engine/src/db/audit.rs
// Audit log writes — compile-time checked via sqlx.

// TODO: Implement
// - insert_audit_entry(pool, entry) -> AuditLogEntry
// - list_audit_entries(pool, namespace, limit, offset) -> Vec<AuditLogEntry>
// - get_audit_entry(pool, id) -> Option<AuditLogEntry>
