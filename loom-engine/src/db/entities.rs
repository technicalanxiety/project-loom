// loom-engine/src/db/entities.rs
// Entity CRUD + resolution queries — compile-time checked via sqlx.

// TODO: Implement
// - insert_entity(pool, entity) -> Entity
// - find_exact(pool, name, entity_type, namespace) -> Option<Entity>
// - find_by_alias(pool, alias, entity_type, namespace) -> Vec<Entity>
// - find_by_embedding(pool, embedding, entity_type, namespace, threshold) -> Vec<(Entity, f64)>
// - update_aliases(pool, entity_id, aliases)
// - get_entity(pool, id) -> Option<Entity>
// - list_by_namespace(pool, namespace) -> Vec<Entity>
