// loom-engine/src/db/dashboard.rs
// Read-only dashboard data queries — compile-time checked via sqlx.

// TODO: Implement
// - get_pipeline_health(pool) -> PipelineHealth
// - get_entity_counts(pool, namespace) -> EntityCounts
// - get_fact_counts(pool, namespace) -> FactCounts
// - get_compilation_traces(pool, namespace, limit, offset) -> Vec<AuditLogEntry>
// - get_conflict_queue(pool) -> Vec<ResolutionConflict>
// - get_predicate_candidates(pool) -> Vec<PredicateCandidate>
// - get_retrieval_metrics(pool, namespace) -> RetrievalMetrics
