// loom-engine/src/db/predicates.rs
// Predicate registry + pack queries — compile-time checked via sqlx.

// TODO: Implement
// - get_predicates_for_packs(pool, packs: &[String]) -> Vec<PredicateEntry>
// - get_all_packs(pool) -> Vec<PredicatePack>
// - get_pack_predicates(pool, pack) -> Vec<PredicateEntry>
// - track_candidate(pool, predicate, fact_id)
// - resolve_candidate(pool, candidate_id, mapped_to, promoted_to_pack)
// - list_candidates(pool) -> Vec<PredicateCandidate>
