// loom-engine/src/db/procedures.rs
// Procedure queries — compile-time checked via sqlx.

// TODO: Implement
// - insert_procedure(pool, procedure) -> Procedure
// - get_promoted_procedures(pool, namespace, min_confidence) -> Vec<Procedure>
// - update_observation(pool, procedure_id, episode_id)
// - promote_procedure(pool, procedure_id)
