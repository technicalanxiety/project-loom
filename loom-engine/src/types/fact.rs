// loom-engine/src/types/fact.rs
// Fact structs + evidence types.

// TODO: Implement
// - Fact struct (id, subject_id, predicate, object_id, namespace, valid_from, valid_until, etc.)
// - ExtractedFact struct (from LLM response)
// - EvidenceStatus enum (user_asserted, observed, extracted, inferred, promoted, deprecated, superseded)
// - EvidenceStrength enum (explicit, implied)
// - TemporalMarkers struct (valid_from, valid_until)
