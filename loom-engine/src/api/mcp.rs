// loom-engine/src/api/mcp.rs
// MCP JSON-RPC endpoint (loom_think, loom_learn, loom_recall).
// Model Context Protocol interface for AI assistant integration.

// TODO: Implement
// - mcp_handler(State, Json<McpRequest>) -> Json<McpResponse>
// - handle_loom_learn(pool, params) -> LearnResponse
// - handle_loom_think(pool, llm_client, params) -> ThinkResponse
// - handle_loom_recall(pool, params) -> RecallResponse
