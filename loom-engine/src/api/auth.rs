// loom-engine/src/api/auth.rs
// Bearer token middleware (tower).
// Validates LOOM_BEARER_TOKEN on all API requests.

// TODO: Implement
// - auth_middleware(bearer_token) -> tower::Layer
// - validate_token(request, expected_token) -> Result<Request, Response>
