// loom-engine/src/llm/extraction.rs
// Entity + fact extraction prompt execution (Gemma 4 26B MoE via Ollama).
// Prompts loaded from prompts/ directory at startup.

// TODO: Implement
// - extract_entities(client, episode_content) -> Vec<ExtractedEntity>
// - extract_facts(client, episode_content, entities, predicates) -> Vec<ExtractedFact>
// - load_prompt_template(path) -> String
// - assemble_fact_prompt(template, predicate_packs) -> String
