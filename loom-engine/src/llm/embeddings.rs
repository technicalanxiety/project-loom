// loom-engine/src/llm/embeddings.rs
// nomic-embed-text embedding generation (768 dimensions) via Ollama.

// TODO: Implement
// - embed_text(client, text) -> Vec<f64> (768d)
// - embed_batch(client, texts) -> Vec<Vec<f64>>
// - embed_entity(client, name, context) -> Vec<f64>
