// loom-engine/src/pipeline/offline/state.rs
// Derived state computation + tier management.
// Manages hot/warm tier promotion and demotion rules.

// TODO: Implement
// - compute_salience(pool, entity_id) -> f64
// - promote_to_hot(pool, item_id, item_type)
// - demote_from_hot(pool, item_id, item_type)
// - enforce_hot_tier_budget(pool, namespace, budget_tokens)
// - check_promotion_criteria(pool, item_id) -> bool
