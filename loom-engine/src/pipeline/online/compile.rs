// loom-engine/src/pipeline/online/compile.rs
// Package compilation (XML structured + JSON compact).
// Assembles ranked candidates into context packages for AI consumption.

// TODO: Implement
// - compile_package(ranked, namespace, format, hot_tier) -> CompiledPackage
// - format_structured_xml(items, namespace, task_class) -> String
// - format_compact_json(items, namespace, task_class) -> String
// - count_tokens(text) -> usize
// - inject_hot_tier(pool, namespace) -> Vec<HotTierItem>
